{
  "name": "enterprise-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently --names api,web --prefix-colors blue,green --kill-others-on-fail \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "seed": "npm run build --prefix backend && node backend/dist/scripts/seed.js"
  },
  "workspaces": [
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "mongodb-memory-server": "^9.5.0",
    "vite": "^5.4.21"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
